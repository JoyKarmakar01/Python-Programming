# Creating and using a tuple
coordinates = (10.0, 20.0)
print(coordinates[0])      # Output: 10.0

# Tuples are immutable; they cannot be changed after creation
# coordinates[0] = 5.0  # This will raise an error
